<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!--
      The launch theme, shown from the tap until the WebView's first frame.

      Theme.SplashScreen draws the icon on the background on every API level:
      the system splash on Android 12 and later, and a compatible drawable on
      older ones. Capacitor's own BridgeActivity switches to AppTheme.NoActionBar
      in onCreate, so nothing here has to hand over. The stock template pointed
      android:background at a splash.png that was Capacitor's blue placeholder
      and, on Android 12 and later, was never shown at all.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
    </style>
</resources>